API reference
=============

.. note::

   Populated as the modules land (M1 onwards). Currently the package exposes
   only its version and app config.

.. automodule:: paypal_checkout
   :members:
   :undoc-members:
